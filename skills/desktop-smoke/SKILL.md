---
name: desktop-smoke
description: Windows 桌面應用煙霧驗證流程。當需要在修改 WinForm 或 WPF 應用後驗證行為，或使用者要求測試桌面程式、檢查視窗程式的互動與畫面時使用。
---

# Desktop Smoke Verification

此 Skill 用於完成 WinForm 或 WPF 桌面應用變更後的低成本煙霧驗證。它不是完整 E2E 測試框架，不負責新增可重跑的測試案例；若使用者要求建立正式測試，改依專案既有測試架構處理。

## 啟動條件

符合任一條件時套用：

- 使用者明確要求「測這個 WinForm / WPF」、「測一下桌面程式」、「檢查視窗程式」。
- 本次修改影響 WinForm / WPF 的視窗、控制項、互動或繪製邏輯。
- 由 `integration-verify` 判定為桌面應用專案後路由進入。

以下情境不強制套用：

- 只修改與 UI 無關的內部邏輯、文件或設定。
- 專案已有明確的 CI 桌面 UI 測試，且本輪只需執行該測試。

## 驗證流程

1. **確認驗證目標**：本次修改涉及的視窗、控制項或操作流程。若無法判斷，回報需要使用者指明。
2. **建置**：執行 `dotnet build`，確認編譯通過。編譯失敗先修正再繼續。
3. **判斷可用驗證層級**：依下節「驗證層級」由高至低判斷，採用最高可用層級。
4. **依層級執行驗證**。

## 驗證層級

AI 看不到桌面視窗，實際能做到的程度取決於環境提供的工具。依序判斷，採用最高可用層級。

### 層級 A：環境提供桌面 UI 操作工具

當前環境若提供桌面 UI 操作工具（如基於 FlaUI 的 MCP server 所暴露的工具），優先使用，比照 `browser-smoke` 操作瀏覽器的方式：

- 讀取 UI Tree，定位本次修改相關的控制項。
- 模擬點擊、輸入、視窗與分頁切換。
- 確認狀態變更有合理回饋、無未預期的錯誤對話框、版面無明顯異常。

此為一次性即時驗證，不產生可重跑的測試案例。

### 層級 B：專案已有 UI 自動化測試

無桌面操作工具，但專案已整合 FlaUI、WinAppDriver 或 Appium：

- 執行專案既有的 UI smoke / e2e 測試。
- 必要時依專案既有測試架構，協助撰寫涵蓋本次修改的測試。

### 層級 C：無 UI 自動化能力（fallback）

- 啟動存活檢查：以 `dotnet run` 啟動程式，等待數秒後確認 process 仍存活、未設定非零 exit code、stdout / stderr 無未處理例外。這只能證明「啟動後沒有立即崩潰」，不能證明畫面或互動正確。
- 視覺呈現、互動行為、版面正確性 AI 無法驗證。必須誠實回報「桌面 UI 互動需手動驗證」，並列出本次修改相關的手動檢查清單（操作哪個控制項、開哪個視窗、預期看到的結果）交給使用者，不得宣稱已驗證實際未驗證的項目。

提高自動化程度的方向：桌面程式的邏輯與 UI 分離得越好（WPF 採 MVVM、商業邏輯抽離 code-behind），可被單元測試與層級 A / B 涵蓋的比例越高。若要長期提升，可評估在專案導入 FlaUI（.NET 原生、較輕量）或 WinAppDriver + Appium。

## 資料異動安全規範

> 本段為所有驗證流程的共用規範，`integration-verify`、`browser-smoke`、`api-smoke` 內含逐字相同的副本。修改時四處需一併更新。

涉及資料庫或外部副作用時必須遵守：

- 驗證只對**本機或開發環境**執行。偵測到目標指向測試或正式環境時，停止並回報。
- **破壞性或不可逆操作**（刪除資料、付款、發送 email / SMS / 推播等）執行前必須向使用者確認，不得自行執行。
- 多情境測試會批量寫入時，設定建立資料的數量上限。
- 可行時自行清理驗證過程產生的測試資料。

## 修正迴圈

發現問題時先判斷關聯性：

- 與本次修改直接相關：修正後重跑同一組桌面驗證。
- 與本次修改無關：列為既有問題或外部阻塞，不擴大修改範圍。
- 根因不明且需要系統化診斷：停止煙霧驗證，改走 Debug 流程。

修正前必須先講出明確假設：「問題在 X，因為 Y，所以改 Z」。講不出假設、只是換個地方碰運氣時，立刻停止並回報，不消耗修正次數硬試。

修正次數上限針對**同一個問題**計算：

- 同一個未解問題最多嘗試修正 3 次，第 3 次仍失敗就停止並回報該問題。
- 修正的是各自不同、根因明確的問題，且驗證持續前進時，不計入此上限，繼續驗證。
- 若發現的獨立問題數量異常多（明顯超出一次正常變更應有的量），代表本次變更整體品質有疑慮，停止並回報問題清單與此現象，由使用者決定是否繼續。

停止時回報：

- 失敗的視窗或操作。
- 觀察到的錯誤對話框、例外或畫面現象。
- 已修正的項目。
- 尚未解決的原因或需要使用者補充的條件。

## 收尾清理

驗證結束後（成功或中止皆適用）：

- 關閉本流程以 `dotnet run` 自行啟動的桌面程式。
- 層級 A 操作期間開啟的視窗，驗證後關閉。
- 刪除驗證過程產生的臨時檔案。

## 驗證報告格式

完成後用精簡格式回報：

```text
桌面應用煙霧驗證：
- 程式 / 視窗：
- 驗證層級：A / B / C
- 檢查項目與結果：
- 修正迴圈：
- 未解決項目：
```

若沒有問題，`未解決項目` 可省略。採用層級 C 時，必須附上手動檢查清單。若未能執行驗證，必須說明缺少的工具、服務或資料條件。

有未解項目或阻塞條件時的檔案輸出：

- 本流程由 `integration-verify` 呼叫時，未解項目交回由其統一輸出，不自行寫檔。
- 本流程為獨立執行時，將未解項目、阻塞條件與手動檢查清單寫入 `<work-root>/.local/ai-sessions/verify-pending.md`（覆寫模式）。
- 全部通過時不寫檔；若該檔先前已存在，刪除它。
